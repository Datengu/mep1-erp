<Window x:Class="Mep1.Erp.Desktop.LinkInvoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Link invoice" Height="520" Width="980"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#FF1E1E1E"
        Foreground="#FFE0E0E0">

    <Window.Resources>

        <!-- Dark DataGrid style for Applications tab -->
        <Style x:Key="DarkDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#FF262626"/>
            <Setter Property="Foreground" Value="#FFE0E0E0"/>
            <Setter Property="RowBackground" Value="#FF262626"/>
            <Setter Property="AlternatingRowBackground" Value="#FF303030"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ColumnHeaderHeight" Value="28"/>
            <Setter Property="RowHeight" Value="26"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Margin" Value="0"/>

            <!-- Selection colors (avoid default bright blue) -->
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="SelectionMode" Value="Single"/>

            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGrid}, Path=Background}"/>
                        <Setter Property="Foreground" Value="#FFE0E0E0"/>
                        <Setter Property="BorderBrush" Value="#FF444444"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF3A3A3A"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>

            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FF333333"/>
                        <Setter Property="Foreground" Value="#FFE0E0E0"/>
                        <Setter Property="BorderBrush" Value="#FF444444"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Wrapped text style for Notes column -->
        <Style x:Key="WrappedCellTextBlock" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
        </Style>

        <!-- Bolder date style -->
        <Style x:Key="BoldDateTextBlock" TargetType="TextBlock" BasedOn="{StaticResource WrappedCellTextBlock}">
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

    </Window.Resources>

    <Grid Background="#FF1E1E1E" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Link invoice to application" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding HeaderLine}" Foreground="#666" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Filters -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Project"/>
            <TextBox Grid.Column="1" Text="{Binding ProjectCode, Mode=OneWay}" IsReadOnly="True"/>
            
            <TextBlock Grid.Column="3" VerticalAlignment="Center" Text="Client"/>
            <TextBox Grid.Column="4" Text="{Binding ClientName, Mode=OneWay}" IsReadOnly="True"/>
            
            <TextBlock Grid.Column="6" VerticalAlignment="Center" Text="Search"/>
            <TextBox Grid.Column="7" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

            <CheckBox Grid.Column="9" VerticalAlignment="Center" Content="Unlinked only"
                      IsChecked="{Binding UnlinkedOnly}"/>

            <TextBlock Grid.Column="10" VerticalAlignment="Center" HorizontalAlignment="Right"
                       Text="{Binding ResultsCountText}" Foreground="#666"/>
        </Grid>

        <!-- Grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding InvoiceView}"
                  SelectedItem="{Binding SelectedRow}"
                  Style="{StaticResource DarkDataGridStyle}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice" Binding="{Binding InvoiceNumber}" Width="90"/>
                <DataGridTextColumn Header="Date" Binding="{Binding InvoiceDate, StringFormat=dd/MM/yyyy}" Width="95"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/>

                <DataGridTextColumn Header="Net" Binding="{Binding NetAmount, StringFormat=N2}" Width="95"/>
                <DataGridTextColumn Header="Outstanding" Binding="{Binding OutstandingNet, StringFormat=N2}" Width="105"/>
                <DataGridTextColumn Header="Δ Net" Binding="{Binding DeltaNet, StringFormat=N2}" Width="95"/>

                <DataGridTextColumn Header="Job" Binding="{Binding JobName}" Width="*"/>
                <DataGridTextColumn Header="Linked?" Binding="{Binding IsLinkedText}" Width="75"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <DockPanel Grid.Row="3" Margin="0,12,0,0" LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Foreground="#666"
                       Text="Select an invoice then click Link."/>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" MinWidth="90" Margin="0,0,10,0" IsCancel="True"/>
                <Button Content="Link selected" MinWidth="120" IsDefault="True"
                        Click="LinkSelected_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
